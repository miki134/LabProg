#include<iostream>
#include<numeric>
#include<vector>
#include<string>


int main()
{
	std::cout << __cplusplus;

	/*
	std::vector<int> v1(14);//14-to elementowy wektor zer ca³kowitych
	std::iota(v1.begin(), v1.end(), 3);//wektor liczb ca³kowitych od 3 do 16

	std::vector<double> v2(14);//14-to elementowy wektor zer zmiennoprzecinkowych
	std::iota(v2.begin(), v2.end(), 3.1);//wektor liczb zmiennoprzecinkowych od 3.1 do 16.1

	std::vector<char> v3(14);//14-to elementowy wektor znaków 0
	std::iota(v3.begin(), v3.end(), 'a');//wektor znaków od 'a' do 'n'

	std::vector<std::string> v4(14);//14-to elementowy wektor znaków ' '
	std::iota(v4.begin(), v4.end(), "abcdefghijklmnop");
	//wektor napisów:  "abcdefghijklmnop" , "abcdefghijklmno", "abcdefghijklmn", ... "nop"

	for (int i : v1)
		std::cout << i << std::endl;

	for (std::string i : v4)
		std::cout << i << std::endl;

		*/
	return 0;
}
